version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reading-debrid-api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
